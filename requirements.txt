# Core web app — works without a GPU / transcription stack.
fastapi>=0.110
uvicorn[standard]>=0.27
requests>=2.31
python-multipart>=0.0.9
# Moodle import uses the official mobile web-service API (token-based), so no
# browser-cookie reading or HTML scraping is required.
