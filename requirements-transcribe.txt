# Optional heavy extras for actual transcription + PDF conversion.
# Install these in addition to requirements.txt when you want to transcribe.
#
#   pip install -r requirements-transcribe.txt
#
# This meta file pulls the STT base pack (faster-whisper + yt-dlp + markitdown).
# Optional capability packs (install only what you need):
#
#   requirements-stt-quality.txt     Granite / Qwen3 (transformers + torch)
#   requirements-stt-speakers.txt    pyannote diarization (needs HF token once)
#   requirements-stt-live.txt        Moonshine streaming (useful-moonshine)
#   requirements-stt-specialist.txt  FireRedASR / Omnilingual long-tail

-r requirements-stt-base.txt
